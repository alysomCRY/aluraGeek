function alterarImagem(imagem) {
    // Obter a imagem atualmente exibida
    var imagemElemento = document.getElementById('imagem');

    // Alterar o atributo "src" da imagem para a nova imagem
    imagemElemento.src = imagem;
 }